"""Model-agnostic LLM tool-calling layer: a common client interface, the Groq
client (native tool calling + JSON fallback), a strict JSON parser, and the
shared prompt templates used identically by every evaluated model.
"""
