"""Controlled function-calling core: tool models, registry (validation +
execution + logging), the normalization layer, and the SQL-backed FM functions
that are the only callables the LLM may invoke.
"""
