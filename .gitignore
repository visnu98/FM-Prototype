# ─────────────────────────────────────────────────────────────────────────────
# Secrets — NEVER commit. Credentials live only in .env (use .env.example).
# ─────────────────────────────────────────────────────────────────────────────
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# OS / editor
.DS_Store
Thumbs.db
.idea/

# Claude Code local project settings.
.claude/

# Generated / transient evaluation artifacts (regenerated by the runner). The
# committed reference is data/evaluation/ground_truth.json (kept on purpose).
# To share a specific run with the examiner, force-add it: `git add -f <dir>`.
data/logs/
data/evaluation/runs/
data/evaluation/_run.log
